#include<iostream>
#include<vector>

#define rep(i, n) for(int i=1; i<=n; i++)
using namespace std;

int main(){
    int N, A;
    cin >> N >> A;
    vector<int>T(N+1,0);
    rep(i,N) cin >> T[i];

    rep(i,N){
        
    }

    return 0;
}