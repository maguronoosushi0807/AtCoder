#include<iostream>
#include<vector>

#define rep(i, n) for(int i=1; i<=n; i++)
using namespace std;

int main(){
    int N,K;
    cin >> N >> K;
    vector<int> P(N+1, 0);
    rep(i,N) cin >> P[i];

    return 0;
}